{
  "name": "ffmpeg-js",
  "version": "1.0.1",
  "description": "A lightweight ffmpeg webassembly",
  "types": "src/index.ts",
  "main": "dist/index.js",
  "keywords": [
    "ffmpeg",
    "ffmpeg-js",
    "webassembly",
    "wasm"
  ],
  "scripts": {
    "prepublish": "npm run build",
    "start": "cd example && ts-node ./www",
    "build": "tsc && tsc --allowJs assembly/audio.js --out dist/audio.js --noEmitOnError && node apply.js && npm run umd",
    "umd":"browserify --entry dist/src/index.js --noparse=$PWD/dist/assembly/audio.js  --no-bundle-external --ignore-missing  --outfile dist/umd/ffmpegjs.js",
    "dev": "watchify --no-bundle-external --entry src/index.ts --outfile dist/umd/ffmpegjs.js -p [ tsify --noImplicitAny ]",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "node-web-mvc": "^2.13.9",
    "reflect-metadata": "^0.1.13",
    "@types/node": "^17.0.21",
    "tsify": "^5.0.4",
    "watchify": "^4.0.0",
    "ts-node": "^8.10.2",
    "typescript": "^4.3.5"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {}
}